/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "upload.wikimedia.org",
            "media.licdn.com"
        ],
    },
};

module.exports = nextConfig